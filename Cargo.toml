[package]
name = "git-fixme"
version = "0.1.0"
authors = ["Marcel Hollerbach <mail@marcel-hollerbach.de>"]

[dependencies]
docopt = "1"
serde = "1.0" # if you're using `derive(Deserialize)`
serde_derive = "1.0" # if you're using `derive(Deserialize)`
git2 = "0.7"
